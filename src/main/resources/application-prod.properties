# ===== PRODUCTION CONFIGURATION =====
server.port=${PORT:8080}

# MySQL on Clever Cloud (Using IP Address)
spring.datasource.url=jdbc:mysql://10.171.225.185:3306/bmbnsoxtxjrmbg5wzlgh?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&connectTimeout=30000&socketTimeout=30000
spring.datasource.username=uxnnqgba5kyfyo3
spring.datasource.password=A0ocgwukQ771TubV5ty
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HikariCP Connection Pool Settings (Important for external DB)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.keepalive-time=30000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true

# Thymeleaf
spring.thymeleaf.cache=true

# Session
server.servlet.session.timeout=30m

# Logging
logging.level.com.example.Employee_OutPass_Project=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.org.springframework.web=WARN